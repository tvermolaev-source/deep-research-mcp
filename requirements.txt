# MCP server dependencies
mcp[cli]>=1.0.0
starlette>=0.40.0
# uvicorn>=0.32 нужен, чтобы был параметр host_header_validation.
# В нашем server.py мы НЕ зависим от этого параметра (отключаем Host-валидацию
# через ASGI-middleware, который работает на любой версии), но держим
# минимальную версию >=0.32, чтобы при обновлениях ничего не сломалось.
uvicorn[standard]>=0.51.0
sse-starlette>=2.1.0
pydantic>=2.5.0

# HTTP client
httpx>=0.27.0
aiohttp>=3.9.0

# Crawl4AI (как библиотека)
crawl4ai>=0.3.0

# Utilities
python-dotenv>=1.0.0
tenacity>=8.2.0
